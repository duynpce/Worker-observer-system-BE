spring:
  application:
    name: worker-observer-system

  datasource:
    url: jdbc:postgresql://localhost:5432/worker_observer_system_db
    username: postgres
    password: password

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate:
      ddl-auto: update

server:
  servlet:
    context-path: /api


